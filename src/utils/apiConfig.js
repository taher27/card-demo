const URL = "http://localhost:3030";
const card_url = `${URL}/card`;

// Card
export const getAllCardData = `${card_url}`;
export const addCardData = `${card_url}/add`;
export const deleteCardData = `${card_url}/delete`;